let initialState = {
    friends: [
        {id: 1, name: 'Pasha'},
        {id: 2, name: 'Dima'},
        {id: 3, name: 'Igor'},
    ],
}

const sidebarReducer = (state = initialState, action) => {


    return state;
}
export default sidebarReducer;